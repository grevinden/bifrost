{
  "$schema": "https://www.getbifrost.ai/schema",
  "providers": {
    "openai": {
      "keys": [
        {
          "value": "env.OPENAI_API_KEY",
          "models": ["gpt-4o"]
        }
      ]
    }
  },
  "plugins": [
    {
      "enabled": true,
      "name": "llmboster",
      "config": {
        "prompt": "Think step by step, write down your reasoning, then give a final answer."
      }
    }
  ]
}
